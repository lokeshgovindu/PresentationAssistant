<Window x:Class="PresentationAssistant.PresentationAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:PA="clr-namespace:PresentationAssistant"
        xmlns:Shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
        mc:Ignorable="d" 
        Width="Auto" Height="Auto"
        ShowActivated="False"
        ShowInTaskbar="False"
        Focusable="False"
        Background="#FFDCF2DC"
        Foreground="{DynamicResource {x:Static Shell:VsBrushes.PanelTextKey}}"
        FontSize="24"
        FontFamily="Segoe UI"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        AllowsTransparency="True"
        Opacity="0.8">
    
    <!--Background="{DynamicResource {x:Static Shell:VsBrushes.AccentLightKey}}"-->

    <Border BorderThickness="1"
            BorderBrush="{DynamicResource {x:Static Shell:VsBrushes.AccentBorderKey}}"
            Padding="10 5 10 5">
        <StackPanel Name="CommandText"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
        </StackPanel>
    </Border>
</Window>
